import { ReactNode } from "react";

interface Props {
    children: ReactNode;
  }

export default function Expenses({ children }: Props) {
    return (
      <>
        <h2>cart</h2>
      </>
    );
  }